{
  "name": "ai-workflow-canvas",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n server,frontend -c blue,green \"npm run dev:server\" \"npm run dev:frontend\"",
    "dev:server": "npm --prefix server run dev",
    "dev:frontend": "npm --prefix ai-workflow-canvas-vue run dev -- --host"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
