{
  "name": "ai-workflow-canvas",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n server,frontend,admin -c blue,green,yellow \"npm run dev:server\" \"npm run dev:frontend\" \"npm run dev:admin\"",
    "dev:server": "npm --prefix server run dev",
    "dev:frontend": "npm --prefix ai-workflow-canvas-vue run dev -- --host",
    "dev:admin": "npm --prefix admin-app run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
