# AI画布 管理后台操作指南

## 简介

AI画布是一个面向区县融媒体中心的AI内容生产平台。管理后台用于管理渠道商、融媒体中心、用户账号和算力资源。

- **画布前端**（用户使用）：`http://163.61.202.138:18080`
- **管理后台**（运营管理）：`http://163.61.202.138:18080/admin`
- **默认账号**：`admin`，密码：`111111`

## 组织架构

平台采用三层架构：

```
平台（总后台）
  └── 租户（渠道商，如：索贝公司）
        ├── 主体（融媒体中心，如：湟中融媒）
        │     ├── 用户（编辑记者）
        │     └── 用户（编辑记者）
        ├── 主体（融媒体中心，如：海北融媒）
        │     └── 用户（编辑记者）
        └── ...
```

| 层级 | 是什么 | 举例 |
|------|--------|------|
| **租户** | 渠道商/代理商，面向多个融媒体中心 | 索贝公司 |
| **主体** | 区县融媒体中心，实际使用AI能力的单位 | 湟中融媒、海北融媒 |
| **用户** | 融媒体中心里的编辑、记者 | 张编辑、李记者 |

## 管理后台有两种角色

| 角色 | 谁来用 | 管什么 |
|------|--------|--------|
| **总后台管理员** | 平台运营方 | 管理所有渠道商、创建租户管理员账号、给渠道商充值。**也可以通过顶部租户选择器直接管理任意租户内的所有事务** |
| **租户管理员** | 渠道商（租户）的管理者 | 管理自己旗下的融媒体中心、给各中心分配算力、管理用户 |

> `admin` 是总后台管理员。租户管理员由总后台创建。
>
> **设计的妙处**：总后台和租户后台是同一个系统，只是角色不同。总后台通过顶部「租户选择器」切换到任意渠道商，就能以该渠道商的视角管理所有事务（主体、用户、钱包、定价），无需切换账号。租户管理员登录后只能看到自己租户，看不到其他租户。一套系统，两种视角。

---

## 一、总后台管理员操作指南

### 首页：数据看板

- 平台整体数据：租户总数、活跃数、平台充值总额、消耗总额
- 近7天消耗趋势、租户算力排行

### 核心功能：以总后台身份管理任意渠道商

总后台页面顶部有一个**租户选择器**，可以从下拉菜单中搜索并选择任意渠道商。选择后，以下页面会自动切换到该渠道商的视角：

- 「主体管理」— 查看和操作该渠道商旗下的融媒体中心
- 「用户管理」— 管理该渠道商旗下的用户
- 「钱包流水」— 查看该渠道商的算力流水
- 「模型定价」— 设置该渠道商的模型定价

> 也就是说，总后台管理员不需要退出登录、不需要知道租户管理员的密码，就能直接帮任何渠道商处理所有管理事务。这是总后台和租户后台**一体设计**的核心价值。

### 场景一：新增一个渠道商（租户）

某公司成为平台的渠道代理商，需要为其创建租户。

1. 点击「租户管理」→「新建租户」
2. 填写：
   - **名称**：渠道商公司名，如「索贝数码」
   - **联系人**、**电话**：渠道商负责人信息
   - **用户上限**：该渠道商旗下最多可容纳的用户数（填0不限）
   - **主体上限**：该渠道商旗下最多可创建的融媒体中心数（填0不限）
3. 保存

> 系统会自动生成编码，无需手动填写。

### 场景二：给渠道商充值和回收算力

渠道商需要算力资源才能向下分配。

1. 在「租户管理」列表找到目标渠道商
2. 点击「充值」
3. 输入金额：
   - **正数** = 充值，如输入 `1000`
   - **负数** = 回收，如输入 `-500`（需租户余额充足）
4. 确认

> 充值后，渠道商管理员就可以将算力分配给旗下的融媒体中心。回收时算力从渠道商退回平台。

### 场景三：创建渠道商管理员账号

1. 点击「账号管理」→「新建用户」
2. 填写：
   - **用户名**：如 `sobe_admin`
   - **密码**：如 `111111`
   - **角色**：选择「租户管理员」
   - **租户**：在下拉框中选择目标渠道商（输入渠道商名称可搜索）
3. 保存后将用户名密码发给对方

> 渠道商管理员用这个账号登录后，只能看到自己租户范围内的数据。

---

## 二、租户管理员（渠道商）操作指南

渠道商管理员登录后，顶部会实时显示：
- **剩余算力**：还可以分配给融媒体中心的算力
- **剩余主体**：还可以创建的融媒体中心数量
- **剩余用户**：还可以纳入的用户数量

### 首页：数据看板

- 可用算力、已分配算力、累计获得算力
- 今日/昨日/近7天/本月消耗
- 消耗趋势图

### 场景四：新增一个融媒体中心（主体）

某区县融媒体中心接入平台。

1. 点击「主体管理」→「新建主体」
2. 填写名称，如「湟中融媒」
3. 保存

### 场景五：给融媒体中心分配/回收算力

1. 在「主体管理」列表中找到目标融媒体中心
2. 点击「分配/回收」
3. 输入金额：
   - **正数** = 分配，如 `300`（从渠道商余额扣，加到融媒体中心）
   - **负数** = 回收，如 `-200`（从融媒体中心扣，退回渠道商。需融媒体中心余额充足）
4. 确认

### 场景六：给用户分配/回收算力

**分配**用正数，**回收**用负数（需用户余额充足），回收后算力退回来源方。

**方式一：从渠道商直接分配/回收**

1. 点击「用户管理」
2. 找到目标用户，点击「分配/回收」
3. 来源选「租户余额」，输入金额（正=分配，负=回收），确认

**方式二：从融媒体中心分配/回收（推荐）**

1. 点击「主体管理」
2. 找到目标融媒体中心，点击「主体→用户」
3. 选择用户，输入金额（正=分配，负=回收），确认

> 推荐方式二：先把算力拨给融媒体中心（场景五），再由该中心自行管理分配和回收，权责清晰、流水可追溯。

### 场景七：将用户绑定到融媒体中心

用户默认没有归属主体，需要手动绑定。

1. 点击「用户管理」
2. 找到目标用户，点击「编辑」
3. 选择所属主体，保存

### 场景八：查看钱包流水

1. 点击「钱包流水」
2. 可按类型筛选：平台充值、平台回收、分配→主体、主体回收→租户、分配→用户、用户回收→租户、主体→用户、用户退回→主体、用户消耗、生成退款

流水颜色含义：<span style="color:#67C23A">绿色</span> = 租户入账，<span style="color:#F56C6C">红色</span> = 租户出账，<span style="color:#909399">灰色</span> = 不影响租户（如主体内部转移、用户消耗）

### 场景九：调整模型定价

如果希望旗下融媒体中心使用模型时消耗更多或更少算力：

1. 点击「模型定价」
2. 点击「设置定价」
3. 选择模型，输入定价（不可低于平台基准价）
4. 保存

---

## 常见问题

**Q：忘记账号密码怎么办？**

请联系总后台管理员重置。

**Q：算力不够了怎么办？**

渠道商（租户管理员）联系平台方（总后台）充值。融媒体中心或用户联系所在渠道商的管理员。

**Q：创建融媒体中心时提示达到上限？**

联系总后台管理员，调高该渠道商的「主体上限」。

**Q：两边的余额对不上？**

总后台看到的渠道商余额 = 充值总额 − 已分配给下属单位和用户的总额。如果数字不匹配，刷新页面即可。

---

*最后更新：2026年5月7日*
